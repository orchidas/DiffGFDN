trainer_config:
    device: 'mps'
    max_epochs: 3
    train_dir: '../output/svf_pole_compress/'
    ir_dir: '../audio/svf_pole_compress/'
output_filter_config:
    # by how much to reduce the poles of the output gain SVFs
    compress_pole_factor: 0.95 